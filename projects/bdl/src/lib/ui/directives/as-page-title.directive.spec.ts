import { AsPageTitleDirective } from './as-page-title.directive';

describe('AsPageTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new AsPageTitleDirective();
    expect(directive).toBeTruthy();
  });
});
