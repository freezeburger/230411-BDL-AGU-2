import { InactiveDirective } from './inactive.directive';

describe('InactiveDirective', () => {
  it('should create an instance', () => {
   /*  const directive = new InactiveDirective();
    expect(directive).toBeTruthy(); */
    expect(true).toBeTruthy();
  });
});
