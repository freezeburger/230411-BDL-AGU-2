import { RevealMenuDirective } from './reveal-menu.directive';

describe('RevealMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealMenuDirective();
    expect(directive).toBeTruthy();
  });
});
