import { UiImageEmphasisDirective } from './ui-image-emphasis.directive';

describe('UiImageEmphasisDirective', () => {
  it('should create an instance', () => {
    const directive = new UiImageEmphasisDirective();
    expect(directive).toBeTruthy();
  });
});
