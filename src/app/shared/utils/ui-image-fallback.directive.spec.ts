import { UiImageFallbackDirective } from './ui-image-fallback.directive';

describe('UiImageFallbackDirective', () => {
  it('should create an instance', () => {
    /* const directive = new UiImageFallbackDirective();
    expect(directive).toBeTruthy(); */
  });
});
