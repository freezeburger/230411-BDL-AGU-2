import { RetractedDirective } from './retracted.directive';

describe('RetractedDirective', () => {
/*   it('should create an instance', () => {
    const directive = new RetractedDirective();
    expect(directive).toBeTruthy();
  }); */
});
